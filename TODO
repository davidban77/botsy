
Todo:
  ☐ Accessing the `self.income` can be less than zero but need to specify a limit or threshold to fire up an exception @high
  ☐ Need to create the methology for the `saving buckets` @high
  ☐ Need to raise a method to update the categories available in `data/categories.yml` in a more permanent manner @high
